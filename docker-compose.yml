version: '3'
services:
  db:
    image: mongo
    volumes:
      - mongo-data:/data/db
  api:
    build: .
    ports:
      - "8080:8080"
    links:
      - db
    depends_on:
      - db
volumes:
  mongo-data: